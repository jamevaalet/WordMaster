@model IEnumerable<WordMaster.Models.WordUsage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Usage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Usage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WordUsageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WordUsageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WordUsageID })
        </td>
    </tr>
}

</table>
